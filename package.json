{
  "name": "skullpwapi",
  "version": "2.0.0",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "init-db": "node init-db.js"
  },
  "dependencies": {
    "@libsql/client": "^0.5.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}